options {
  directory "/var/bind";

	listen-on { any; };
	listen-on-v6 { any; };

  pid-file "/var/run/named/named.pid";

  allow-recursion { none; };
  recursion no;

  allow-transfer { any; };
};

logging {
    channel stdout_errors {
        stderr;
        severity error;

        print-category yes;
        print-severity yes;
        print-time yes;
    };

    category default { stdout_errors; };
};

zone "test.tidor.net" {
  type primary;
  file "test.tidor.net.signed";
};
